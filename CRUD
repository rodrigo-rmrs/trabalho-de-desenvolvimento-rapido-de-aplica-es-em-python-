import mysql.connector

conexao = mysql.connector.connect(
    host="localhost",
    user="root",
    password="1234",
    database="teste"
)

cursor = conexao.cursor()

cursor.execute("""
CREATE TABLE IF NOT EXISTS clientes (
    id INT AUTO_INCREMENT PRIMARY KEY,
    nome VARCHAR(100),
    email VARCHAR(100)
)
""")

cursor.execute("""
INSERT INTO clientes (nome, email)
VALUES ('Rodrigo', 'rodrigo@gmail.com')
""")

cursor.execute("""
UPDATE clientes
SET email = 'novoemail@gmail.com'
WHERE id = 1
""")

conexao.commit()

print("Dados inseridos e atualizados.")

conexao.close()
